#include "main.h"
/**
 * _sqrt_recursion - returns the natural square root of a number;
 * @n: variable
 * Return: If n doesn't have a natural square root, return -1, else the velue.
 */
int _sqrt_recursion(int n)
{

}
